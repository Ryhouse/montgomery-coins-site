import React from "react";
import ReactDOM from "react-dom/client";
import MontgomeryCoins from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MontgomeryCoins />
  </React.StrictMode>
);
